<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 选中态：语义色 surface_2 作为高亮底 -->
    <item android:state_selected="true" android:drawable="@color/aw_surface_2" />
    <!-- 默认态：具体 ripple 背景（不引用 ?attr/，避免 theme=null 解析失败） -->
    <item android:drawable="@drawable/nav_item_ripple" />
</selector>
